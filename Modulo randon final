# Importamos el módulo "randon" que nos permite generar números aleatorios.
import random

numero_aleatorio = random.randint(1, 100) # "randint" devuelve un número entero incluido el 1  y el 100. 

# Se imprime el resultado
print("El número aleatorio generado es:", numero_aleatorio)
